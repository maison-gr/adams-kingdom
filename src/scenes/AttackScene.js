// Placeholder — fully implemented in Step 3.
// Registered here so RewardSystem can launch it immediately.
export class AttackScene extends Phaser.Scene {
  constructor() { super('AttackScene'); }
  create()      { /* Step 3 */ }
}
